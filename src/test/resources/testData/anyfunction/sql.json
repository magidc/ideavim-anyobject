{
  "extension": "sql",
  "code": "CREATE PROCEDURE test_proc()<caret_1>\nBEGIN<caret_2>\n    SELECT 1;<caret_3>\nEND;<caret_4>\n\n<caret_5>CREATE FUNCTION add_func(a INT, b INT) RETURNS INT<caret_6>\nBEGIN\n    RETURN a + b;<caret_7>\nEND;",
  "inner": {
    "1": "SELECT 1;",
    "2": "SELECT 1;",
    "3": "SELECT 1;",
    "4": "RETURN a + b;",
    "5": "RETURN a + b;",
    "6": "RETURN a + b;",
    "7": "RETURN a + b;"
  },
  "around": {
    "1": "CREATE PROCEDURE test_proc()\nBEGIN\n    SELECT 1;\nEND;",
    "2": "CREATE PROCEDURE test_proc()\nBEGIN\n    SELECT 1;\nEND;",
    "3": "CREATE PROCEDURE test_proc()\nBEGIN\n    SELECT 1;\nEND;",
    "4": "CREATE FUNCTION add_func(a INT, b INT) RETURNS INT\nBEGIN\n    RETURN a + b;\nEND;",
    "5": "CREATE FUNCTION add_func(a INT, b INT) RETURNS INT\nBEGIN\n    RETURN a + b;\nEND;",
    "6": "CREATE FUNCTION add_func(a INT, b INT) RETURNS INT\nBEGIN\n    RETURN a + b;\nEND;",
    "7": "CREATE FUNCTION add_func(a INT, b INT) RETURNS INT\nBEGIN\n    RETURN a + b;\nEND;"
  }
}
